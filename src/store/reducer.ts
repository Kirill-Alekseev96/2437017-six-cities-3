import { createReducer } from '@reduxjs/toolkit';
import { toggelFavorite } from './action';

export const reducer = createReducer(нач.Сост, (builder) => {
  builder
    .addCase(toggelFavorite, (state) => {

    });
});
